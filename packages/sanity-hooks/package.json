{
  "name": "sanity-hooks",
  "private": true,
  "version": "0.0.1",
  "description": "Sanity specific hooks",
  "main": "./dist/index.js",
  "repository": "https://github.com/gearbox-built",
  "author": "Devin Edwards",
  "license": "UNLICENSED",
  "scripts": {
    "build": "yarn build:sanity-hooks",
    "start": "swc ./src -d dist -w",
    "build:sanity-hooks": "swc ./src -d dist",
    "blast:node": "rm -rf node_modules",
    "blast:dist": "rm -rf dist"
  },
  "dependencies": {
    "@sanity/image-url": "^1.0.1",
    "chokidar": "^3.5.3",
    "client": "*",
    "config": "*"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.2.173"
  }
}
