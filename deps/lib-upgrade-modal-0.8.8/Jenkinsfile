javascriptLibBuild {
    name = "lib-upgrade-modal"
    nodeVersion = 10
    lint = false
    test = true
    publish = true
    installCommand = "yarn && yarn build"
    testCommand = "CI=true yarn test --coverage"
    storybookDeploy = true
    storybookServiceName = "lib-upgrade-modal"
}
