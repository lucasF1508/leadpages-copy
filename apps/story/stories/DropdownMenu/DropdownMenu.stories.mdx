import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs'
import Pinion from '@components/Pinion'
import Link from '@components/Link'
import DropdownMenu from '@components/DropdownMenu'
import { navigation } from '@data'
const nav = ['Home', 'About', 'Components', 'Contact']
const link = {
  linkStyle: 'text',
  condition: 'internal',
  url: '/',
}

export const Component = (args) => (
  <Pinion
    css={{
      '> div': {
        d: 'flex',
        gap: '$4',
      },
    }}
  >
    {nav.map((navLink) => (
      <DropdownMenu
        menu={navigation?.menu}
        Trigger={() => <Link {...link} label={navLink} />}
        {...args}
      />
    ))}
  </Pinion>
)

# DropdownMenu Component

<Meta title="Primitives/Dropdown Menu/Default" component={DropdownMenu} />

<Canvas>
  <Story name="Default">{Component.bind({})}</Story>
</Canvas>

<ArgsTable story="Default" />

This is some documentation explain features or even implementation
