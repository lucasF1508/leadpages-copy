import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs'
import Pinion from '@components/Pinion'
import { CardPost } from '@components/Cards'
import { image } from '@data'
import { argTypesTemplate } from '@storyUtils/helpers'
const link = {
  condition: 'internal',
  url: '/',
  label: 'Read more',
}
const data = {
  avatar: {
    image,
    name: 'Luke Skywalker',
    title: '19 BBY - 6 min read',
  },
  category: 'Article',
  content:
    'Lorem ipsum dolor sit amet, consectetuer adipiscing elit, sed diam nonummy nibh euismod tincidunt ut laoreet dolore magna aliquam erat volutpat eloth eslit.',
  date: 'May 4, 2021',
  heading: 'This is a very great and interesting article to read',
  image,
}

export const Component = (args) => (
  <Pinion maxwidth="base">
    <CardPost {...data} {...args} />
  </Pinion>
)

# Blog Post Cards

View the canvas to ensure that the full card size is rendered.

<Meta
  title="Components/Cards/Blog Post"
  component={CardPost}
  argTypes={argTypesTemplate([
    {
      arg: 'align',
      table: { disable: true },
    },
    { arg: 'layout', table: { disable: true } },
    { arg: 'image', table: { disable: true } },
    { arg: 'avatar', table: { disable: true } },
  ])}
/>

<Canvas>
  <Story name="Blog Post - Simple" args={{ image: null, avatar: null, link }}>
    {Component.bind({})}
  </Story>
  <Story name="Blog Post - Media + Simple" args={{ avatar: null, link }}>
    {Component.bind({})}
  </Story>
  <Story name="Blog Post - Author" args={{ image: null, link: null }}>
    {Component.bind({})}
  </Story>
  <Story name="Blog Post - Media + Author">{Component.bind({})}</Story>
</Canvas>
