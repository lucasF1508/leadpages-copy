import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs'
import Pinion from '@components/Pinion'
import { Card } from '@components/Cards'
import { image } from '@data'
import { argTypesTemplate } from '@storyUtils/helpers'
import { data } from './CardText.stories.mdx'

export const Component = (args) => (
  <Pinion maxwidth="base">
    <Card {...data} {...args} />
  </Pinion>
)

# Media and Text Card Component

View the canvas to ensure that the full card size is rendered for each version.

<Meta
  title="Components/Cards/Media and Text"
  component={Card}
  argTypes={argTypesTemplate([
    {
      arg: 'align',
      description:
        'Change which side the image appears on. Will affect the horizontal layout only.',
      options: ['left', 'right'],
      control: { type: 'inline-radio' },
    },
    { arg: 'layout', table: { disable: true } },
    { arg: 'image', table: { disable: true } },
  ])}
/>

<Canvas>
  <Story name="Vertical" args={{ image }}>
    {Component.bind({})}
  </Story>
  <Story
    name="Horizontal"
    args={{
      align: 'left',
      image,
      layout: 'horizontal',
    }}
  >
    {Component.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Horizontal" />
