import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs'
import Pinion from '@components/Pinion'
import Popover from '@components/Popover'
import { argTypesTemplate } from '@storyUtils/helpers'
const content = 'This could be content for a tooltip or team member bio.'

export const Component = (args) => (
  <Pinion css={{ h: '50vh', bc: 'background', m: 0, pt: '$8' }}>
    <Popover content={content} {...args} />
  </Pinion>
)

# Popover Component

This can be used as a tooltip or in place of a simple modal (ex. team member bio).

This will open on **hover** by default. To make it open on **click** instead, set `hover = false`.

<Meta
  title="Primitives/Popover/Default"
  component={Popover}
  argTypes={argTypesTemplate([
    {
      arg: 'hover',
      description: 'To render a clickable version, set hover to false.',
      control: {
        type: 'boolean',
      },
    },
  ])}
/>

<Canvas>
  <Story name="Default" args={{ hover: true }}>
    {Component.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
