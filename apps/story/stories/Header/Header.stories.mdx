import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs'
import Pinion from '@components/Pinion'
import Header from '@components/Header'
import { navigation } from '@data'

export const Component = (args) => <Header navigation={navigation} {...args} />

# Header Component

<Meta title="Components/Header/Default" component={Header} />

<Canvas>
  <Story name="Default">{Component.bind({})}</Story>
</Canvas>

<ArgsTable story="Default" />

This is some documentation explain features or even implementation
