import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs'
import Pinion from '@components/Pinion'
import NavDrawer from '@components/NavDrawer'
import { navigation } from '@data'

export const Component = (args) => (
  <Pinion>
    <NavDrawer
      menu={navigation?.menu}
      isNavOpen={true}
      setNavOpen={() => true}
      {...args}
    />
  </Pinion>
)

# NavDrawer Component

<Meta title="Components/NavDrawer/Default" component={NavDrawer} />

<Canvas>
  <Story name="Default">{Component.bind({})}</Story>
</Canvas>

<ArgsTable story="Default" />

This is some documentation explain features or even implementation
