import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs'
import Pinion from '@components/Pinion'
import LogoGrid from '@components/LogoGrid'
import { images } from '@data'
const logoImages = images.slice(3)
const logos = logoImages.map((image) => ({ image, url: '/' }))

export const Component = (args) => (
  <Pinion>
    <LogoGrid logos={logos} {...args} />
  </Pinion>
)

# LogoGrid Component

<Meta title="Components/LogoGrid/Default" component={LogoGrid} />

<Canvas>
  <Story name="Default">{Component.bind({})}</Story>
</Canvas>

<ArgsTable story="Default" />

This is some documentation explain features or even implementation
