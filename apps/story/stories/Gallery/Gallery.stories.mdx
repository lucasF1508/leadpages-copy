import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs'
import Pinion from '@components/Pinion'
import Gallery from '@components/Gallery'
import { mockGallery } from '@data/mockDataUtils'
import { images } from '@data'
export const galleryImages = images.slice(0, 3)
const gallery = mockGallery({
  data: galleryImages,
  count: 2,
})

export const Component = (args) => (
  <Pinion>
    <Gallery images={gallery} {...args} />
  </Pinion>
)

# Gallery Component

<Meta title="Components/Gallery/Default" component={Gallery} />

<Canvas>
  <Story name="Default">{Component.bind({})}</Story>
</Canvas>

<ArgsTable story="Default" />

This is some documentation explain features or even implementation
