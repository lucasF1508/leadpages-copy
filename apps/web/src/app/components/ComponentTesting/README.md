# Component Testing

Solo **Integrations** con variant light para pruebas.

## Cómo actualizar la página

Desde `packages/migration`:

```bash
npm run create-component-testing
```

El script reemplaza los componentes por solo Integrations (versión light).
