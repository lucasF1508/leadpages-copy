// Prebuilt with yarn build:data
// import siteMeta from '@public/indices/siteMeta.json'
// import navigation from '@public/indices/siteNavigation.json'
// import footer from '@public/indices/siteFooter.json'

const useSiteIndices = () => ({
  // siteMeta,
  // navigation,
  // footer,
})

export default useSiteIndices
